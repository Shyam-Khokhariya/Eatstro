query Items($searchValue: String) {
  items(where: { name: { contains: $searchValue } }) {
    createdAt
    cuisineType
    desc
    dietaryChoice
    favoriteCount
    id
    isPublished
    name
    photo
    price
  }
}
